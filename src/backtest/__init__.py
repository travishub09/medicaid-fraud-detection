"""src.backtest — backtesting the anomaly / lead detector against the OIG LEIE exclusion list.

Held-out validation: do the leads the pipeline surfaces line up with providers/companies
that were actually excluded by OIG (LEIE)? Run modules with `python -m src.backtest.<module>`.
"""
